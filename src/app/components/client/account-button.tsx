'use client';

import { useAuth } from '@/app/providers';
import Link from 'next/link';

export default function AccountButton() {
  // Компонент більше не потрібен, бо є нижня панель
  // Залишаємо порожнім для сумісності
  return null;
}
